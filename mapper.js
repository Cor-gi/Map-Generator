let textin = 'abcd'
let textout = [...textin]

const mapped = []

textout.forEach((letter => {
    switch (letter) {
        case 'a':
            mapped.push(la)
            break;
        case 'b':
            mapped.push(lb)
            break;
        case 'c':
            mapped.push(lc)
            break;
        case 'd':
            mapped.push(ld)
            break;
        
    }
}))

console.log(mapped)