let textin = 'abcdefg'
let textout = [...textin]

const mapped = []

textout.forEach((letter => {
    switch (letter) {
        case 'a':
            mapped.push(la)
            break;
        case 'b':
            mapped.push(lb)
            break;
        case 'c':
            mapped.push(lc)
            break;
        case 'd':
            mapped.push(ld)
            break;
        case 'e':
            mapped.push(le)
            break;
        case 'f':
            mapped.push(lf)
            break;
        case 'g':
            mapped.push(lg)
            break;
        
    }
}))

console.log(mapped)